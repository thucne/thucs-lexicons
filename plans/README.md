# Implementation Plans

Generated by the improve skill on 2026-06-18 (commit `74aad0a`). Focus: **UI/UX polish via shadcn/ui**, incremental alongside existing MUI.

Execute in order unless dependencies say otherwise. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | shadcn foundation + Geist token bridge | P1 | M | — | DONE |
| 002 | App shell — Command search, nav, theme | P1 | M | 001 | DONE |
| 003 | Home page marketing surfaces | P2 | S | 001 | DONE |
| 004 | Result page states + layout polish | P1 | M | 001 | DONE |
| 005 | QuickMeaning hover popover | P2 | S | 001, 004 | DONE |
| 006 | Remove MUI after shadcn migration | P3 | L | 002–005 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- **002** requires **001** — needs `components.json`, `cn()`, CSS variables, and `next-themes` before shell components.
- **003** and **004** can run in parallel after **001** (no cross-deps).
- **005** should land after **004** so shared `Badge`/`Skeleton` patterns exist on ResultPage.
- **006** is last — only start when no production surface imports `@mui/*`.

## Relationship to prior audit

These plans address the **UI/UX / direction** gap from the `/improve` audit (MUI+Tailwind dual stack, rough loading/empty states, custom `AppMenu` hover). They do **not** replace correctness/security plans (AI error gate, rate limits, verification baseline) — run those in parallel or before **006** if touching the same files.

## Findings considered and rejected

- **Full dark-mode-first redesign** — product already ships light Geist aesthetic; keep light default, support dark via `next-themes`.
- **Replace every MUI icon with Lucide in one PR** — defer to per-plan icon swaps; big-bang risks visual regression.
- **shadcn `--all` install** — install only components each plan needs; avoids unused `ui/` bloat.
